function reset_global_variable

clear global first_time_display
clear global first_time_refresh_display

clear global pre_data_update
clear global pre_time_display
clear global first_time_display
clear global pre_clear_display

clear global serial_data
global serial_data;
serial_data={[],[],[],[],[],[],[],[],[],[]};

% global handle_axes
% handle_axes=axes;

% global handles_gui
% cla(handles_gui.axes_DISPLAY)
% hold(handles_gui.axes_DISPLAY,'on')
end